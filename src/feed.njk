---
permalink: '/feed.xml'
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.name }}</title>
	<subtitle></subtitle>
	<link href="{{ site.url }}{{ permalink }}" rel="self"/>
	<link href="{{ site.url }}/"/>
	<updated>{{ collections.songs | rssLastUpdatedDate }}</updated>
	<id>{{ site.url }}</id>
	<author>
  <name>{{ site.authorName }}</name>
  <email>{{ site.authorEmail }}</email>
	</author>
	{% for post in collections.songs %}
  {% set absolutePostUrl %}{{ site.url }}{{ post.url | url }}{% endset %}
  <entry>
    <title>{{ song.data.title }}</title>
    <link href="{{ absolutePostUrl }}"/>
    <updated>{{ song.date | rssDate }}</updated>
    <id>{{ absolutePostUrl }}</id>
    <content type="html"><![CDATA[
      {{ song.templateContent | safe }}
    ]]></content>
  </entry>
	{% endfor %}
</feed>
